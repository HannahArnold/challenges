//the seagull problem

// party = [1,1,-1,-1,1,-1,1]

// const enoughChips = (party) => {
//      pos = party.filter(a => a > 0)
//      neg = party.filter(a => a < 0)
//      seagulls = pos.length
//      chips = neg.length
//      if (seagulls >= chips){
//          return true
//      }else{
//          return false
//      }
// }
// console.log(enoughChips(party))
